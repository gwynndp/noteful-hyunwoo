/* eslint-disable no-undef */
module.exports = {
    PORT: process.env.PORT || 8000,
    NODE_ENV: process.env.NODE_ENV || 'development',
    DABABASE_URL : process.env.DATABASE_URL || 'postgresql://hyunwoomoon@localhost/noteful'
}